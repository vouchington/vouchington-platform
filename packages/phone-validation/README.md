# @vouchington/phone-validation

`normalizePhoneNumber(value)` returns an E.164 phone number or `null`; `isPhoneNumber(value)` is
the corresponding predicate. Invalid input is data, never an HTTP error.
