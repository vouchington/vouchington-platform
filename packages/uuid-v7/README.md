# @vouchington/uuid-v7

UUIDv7 minting, validation, date extraction, chronological date bounds, and random-suffix base36
conversion. `validateUuidv7` throws for invalid values; `getDateFromUuidv7` returns `null` instead.
